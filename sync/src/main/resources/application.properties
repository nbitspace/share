
# App Props
com.sync.prop.is-sender=true
com.sync.prop.syncinterval=5000
com.sync.prop.batch-count=3
com.sync.prop.request-url=http://localhost:8081

# Server Configuration
server.port=8080
# logging.level.root=INFO

# DataSource Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/db
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate Configuration
spring.jpa.database=POSTGRESQL
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.quartz.job-store-type=memory
# spring.quartz.job-store-type=jdbc
# spring.quartz.properties.org.quartz.jobStore.driverDelegateClass=org.quartz.impl.jdbcjobstore.StdJDBCDelegate

# org.quartz.jobStore.class=org.quartz.simpl.RAMJobStore
# org.quartz.jobStore.class=org.quartz.impl.jdbcjobstore.JobStoreTX
# org.quartz.jobStore.driverDelegateClass=org.quartz.impl.jdbcjobstore.StdJDBCDelegate
# org.quartz.jobStore.dataSource=quartzDataSource


# Additional Configuration (Optional)
# spring.datasource.hikari.*  # HikariCP connection pool configuration
# spring.jpa.properties.*     # Other Hibernate properties

